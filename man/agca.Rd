% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agca.R
\name{agca}
\alias{agca}
\title{Fit anchored geodesic component analysis}
\usage{
agca(
  x,
  k = NULL,
  threshold = NULL,
  margin = c("rank_pareto", "pareto", "none"),
  cdf = NULL,
  anchor = "canonical",
  p = NULL,
  decluster = NULL,
  bootstrap = NULL,
  keep_data = FALSE,
  ties_method = "average",
  seed = NULL
)
}
\arguments{
\item{x}{Numeric matrix or data frame. Larger values are treated as more
extreme in each margin.}

\item{k}{Number of largest radial observations to retain. Specify either
\code{k} or \code{threshold}.}

\item{threshold}{Radial threshold for selecting extremes.}

\item{margin}{Marginal standardization method. The default \code{"rank_pareto"}
uses empirical ranks. Use \code{"pareto"} with \code{cdf}, or \code{"none"} for already
standardized observations.}

\item{cdf}{Optional CDF function or list of CDF functions for
\code{margin = "pareto"}.}

\item{anchor}{\code{"canonical"}, \code{"frechet"}, \code{"principal"}, or a numeric anchor
vector.}

\item{p}{Working reconstruction rank. Defaults to the full tangent rank.}

\item{decluster}{Optional. Use \code{NULL} for no declustering, \code{TRUE} for runs
declustering with \code{run = 1}, a nonnegative integer run length, or a list
with component \code{run}.}

\item{bootstrap}{Optional integer number of bootstrap resamples.}

\item{keep_data}{Logical. If \code{TRUE}, store the standardized data matrix in
the returned object.}

\item{ties_method}{Tie method used by \code{\link[=rank_pareto]{rank_pareto()}}.}

\item{seed}{Optional random seed used when \code{bootstrap} is supplied.}
}
\value{
An object of class \code{"agca_fit"}.
}
\description{
\code{agca()} is the main user interface. It standardizes margins, extracts
large-radius observations, resolves the anchor, and fits anchored geodesic
components to the resulting angular directions.
}
\examples{
data(agca_10d_simulation)
x <- agca_10d_simulation[paste0("X", 1:10)]
fit <- agca(x, k = 100, p = 3)
fit
agca_rank_summary(fit)
}
